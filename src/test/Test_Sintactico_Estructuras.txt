
PROGRAMAVALIDO {

    int FUNCIONCOMPLEJA(cr le int PCR, cv le float PCV) {
        var VARLOCAL;
        VARLOCAL := 1I;
    
        ## 1. SENTENCIA RETURN CON INVOCACIÓN TOF ##
        return (VARLOCAL + 1I); 
        }
    var A, B, RESULTADO, CONTADOR;
    var FVAR, IVAR;
    
    A := 10I;
    B := 20I;
    
    ## 1. ASIGNACIÓN SIMPLE Y EXPRESIONES ARITMÉTICAS ##
    RESULTADO := A + B * 5I; 
    
    ## 2. INVOCACIÓN CON PASAJE DE PARÁMETROS NOMBRADO ##
    RESULTADO := FUNCIONCOMPLEJA(A->PCR, 1.0->PCV);
    
    ## 3. ASIGNACIÓN MÚLTIPLE (Tema 16 y 19) ##
    A, B = 5I, 6I;
    
    ## 4. CLAUSULA IF-ELSE-ENDIF ##
    if (A < B) { 
        print("A ES MENOR"); 
    } else{ 
        print("B ES MENOR"); 
    } endif;
    
    ## 5. CICLO FOR ASCENDENTE Y DESCENDENTE (Tema 15) ##
    for (CONTADOR from 1I to 3I) {
        print(CONTADOR);
    }; 
    for (CONTADOR from 3I to 1I) {
        print(CONTADOR);
    }; 
    
    ## 6. CONVERSIÓN EXPLÍCITA TOF (Tema 29) ##
    FVAR := 10.0 + tof(A);
    
     ## 7. EXPRESIÓN LAMBDA (Tema 27) ##
    (int X) { if (X == 10I) print("LAMBDA OK");} (A)
  
}